# Torch packages are installed by `apostate setup` from the backend-specific wheel index.
# This avoids installing a CUDA wheel into a ROCm environment.
numpy==2.2.6
fsspec==2026.2.0
transformers
datasets
safetensors
optuna
peft
accelerate  # Required for the model-memory preflight.
requests
sentencepiece
protobuf
scipy
textual
